import * as React from 'react'
import { ReactComponent as CloseToastIcon } from '../../styles/themes/komgo/assets/fonts/close-toast.svg'
export const CustomCloseToastIcon = (props: any) => {
  return <CloseToastIcon {...props} />
}
