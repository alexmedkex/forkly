import * as React from 'react'
import { ReactComponent as AlreadySentIcon } from '../../styles/themes/komgo/assets/fonts/already-sent.svg'

export const CustomAlreadySentIcon = (props: any) => {
  return <AlreadySentIcon {...props} />
}
