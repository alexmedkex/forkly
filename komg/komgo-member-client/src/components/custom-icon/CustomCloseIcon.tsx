import * as React from 'react'
import { ReactComponent as CloseIcon } from '../../styles/themes/komgo/assets/fonts/close-doc-state.svg'
export const CustomCloseIcon = (props: any) => {
  return <CloseIcon {...props} />
}
