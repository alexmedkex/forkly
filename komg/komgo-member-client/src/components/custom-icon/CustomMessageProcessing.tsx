import * as React from 'react'
import { ReactComponent as MessageProcessing } from '../../styles/themes/komgo/assets/fonts/message-processing.svg'

export const CustomMessageProcessing = (props: any) => {
  return <MessageProcessing {...props} />
}
