import * as React from 'react'
import styled from 'styled-components'

import { violetBlue } from '../../styles/colors'

const SpanAsLink = styled.span`
  text-align: center;
  cursor: pointer;
  color: ${violetBlue};
  text-decoration: underline;
`

export default SpanAsLink
