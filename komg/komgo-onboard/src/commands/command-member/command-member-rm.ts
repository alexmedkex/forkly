import { CLI } from '../../cli'
import { Config } from '../../config'
import { logger } from '../../utils'

export const commandMemberRm = async (config: Config, cli: CLI) => {
  logger.error('Not implemented yet')
  process.exit(0)
}
