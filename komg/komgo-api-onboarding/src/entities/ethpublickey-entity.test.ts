import { EthPublicKeyEntity } from './ethpublickey-entity'

describe('EthPublicKeyEntity', () => {
  it('is actually instance of EthPublicKeyEntity', () => {
    expect(EthPublicKeyEntity).toBeTruthy()
  })
})
